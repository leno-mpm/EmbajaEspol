
  # Crear prototipo completo

  This is a code bundle for Crear prototipo completo. The original project is available at https://www.figma.com/design/jnsOaMLWsy3gW1pSjLcHoj/Crear-prototipo-completo.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  